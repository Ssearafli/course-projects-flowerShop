<Window x:Class="FlowerShop.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters ="clr-namespace:FlowerShop.UI.Converters"
        xmlns:local="clr-namespace:FlowerShop.UI"
        mc:Ignorable="d"
        Title="Магазин цветов" Height="450" Width="800">
    <Window.Resources>
        <!--<converters:OrderStatusConverter x:Key="OrderStatusConverterr"/>
        <converters:ProductCategoryConverter x:Key="ProductCategoryConverter"/>-->
    </Window.Resources>
    <Grid>
        <TabControl>
            <TabItem Header="Товар">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>


                    <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                        <Button Content="Добавить товар"
                    Width="150"
                    Margin="0 0 10 0"
                    Click="AddProduct_Click"/>
                        <Button Content="Редактировать товар"
                    Width="170"
                    Click="EditProduct_Click"
                    Margin="0 0 10 0"/>
                        <Button Content="Удалить товар" 
                        Width="170" 
                        Click="DeleteProduct_Click" 
                        Margin="0 0 10 0"/>

                    </StackPanel>


                    <DataGrid x:Name="ProductGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id" Binding="{Binding ProductId}" Width="80"/>
                            <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="120"/>
                            <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="100"/>
                            <DataGridTextColumn Header="Описание" Binding="{Binding Product.Description}" Width="100"/>
                            <DataGridTextColumn Header="Цена за 1 ед." Binding="{Binding Price}" Width="200"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="Заказы">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                        <Button Content="Добавить заказ" Width="150" Margin="0 0 10 0" Click="AddOrder_Click"/>
                        <Button Content="Редактировать заказ" Width="170" Margin="0 0 10 0" Click="EditOrder_Click"/>
                        <Button Content="Удалить заказ" Width="170" Margin="0 0 10 0" Click="DeleteOrder_Click"/>
                    </StackPanel>
                    <DataGrid x:Name="OrderGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Id" Binding="{Binding OrderId}" Width="Auto"/>
                            <DataGridTextColumn Header="Имя заказчика" Binding="{Binding CustomerName}" Width="Auto"/>
                            <DataGridTextColumn Header="Номер телефона" Binding="{Binding CustomerPhone}" Width="Auto"/>
                            <DataGridTextColumn Header="Сумма заказа" Binding="{Binding TotalPrice}" Width="Auto"/>
                            <DataGridTextColumn Header="Статус заказа" Binding="{Binding Status}" Width="Auto"/>
                            <DataGridTextColumn Header="Дата заказа" Binding="{Binding OrderDate}" Width="Auto"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
