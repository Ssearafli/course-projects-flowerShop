<Window x:Class="FlowerShop.UI.AddEditOrder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlowerShop.UI"
        xmlns:converters ="clr-namespace:FlowerShop.UI.Converters"
        mc:Ignorable="d"
        Title="Добавление/Редактирование заказа" Height="450" Width="800"  WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007AFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="2"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="5"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}">

                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              Margin="5"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005FB8"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#888888"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Информация о клиенте" Grid.Row="0" Margin="10,10,10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Имя клиента:" 
                           Grid.Column="0" Grid.Row="0" 
                           VerticalAlignment="Center"
                           Margin="0,0,10,5"/>
                <TextBox x:Name="NameBox" 
                         Grid.Column="1" Grid.Row="0" 
                         Height="25"
                         Margin="0,0,0,5"/>

                <TextBlock Text="Телефон:" 
                           Grid.Column="3" Grid.Row="0" 
                           VerticalAlignment="Center"
                           Margin="0,0,10,5"/>
                <TextBox x:Name="PhoneBox" 
                         Grid.Column="4" Grid.Row="0" 
                         Height="25"
                         Margin="0,0,0,5"/>

                <TextBlock Text="Статус:" 
                           Grid.Column="0" Grid.Row="1" 
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                <ComboBox x:Name="StatusBox" 
                          Grid.Column="1" Grid.Row="1" 
                          Height="25"
                          Margin="0,0,0,0"/>

                <TextBlock Text="Дата заказа:" 
                           Grid.Column="3" Grid.Row="1" 
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                <DatePicker x:Name="OrderDatePicker" 
                            Grid.Column="4" Grid.Row="1" 
                            Height="25"
                            Margin="0,0,0,0"/>
            </Grid>
        </GroupBox>

        <GroupBox Header="Состав заказа" Grid.Row="1" Margin="10,5,10,5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"                        
                        BorderThickness="0,0,0,1" 
                        Padding="0,0,0,10" 
                        Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Товар:" 
                                   Grid.Column="0" Grid.Row="0"
                                   Margin="0,0,10,5"/>
                        <TextBlock Text="Количество:" 
                                   Grid.Column="1" Grid.Row="0"
                                   Margin="0,0,10,5"/>

                        <ComboBox x:Name="ProductComboBox" 
                                  Grid.Column="0" Grid.Row="1"
                                  DisplayMemberPath="Name"
                                  SelectedValuePath="ProductId"
                                  Height="25"
                                  Margin="0,0,10,0"/>

                        <TextBox x:Name="QuantityTextBox" 
                                 Grid.Column="1" Grid.Row="1"
                                 Text="1" 
                                 Height="25"
                                 Margin="0,0,10,0"
                                 PreviewTextInput="QuantityTextBox_PreviewTextInput"/>

                        <Button x:Name="AddProductButton" 
                                Grid.Column="3" Grid.Row="1" 
                                Content="Добавить" 
                                Width="80"
                                Height="25"
                                Click="AddProductButton_Click"/>
                    </Grid>
                </Border>

                <DataGrid x:Name="ProductsGrid" 
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          Margin="0,0,0,10"
                          ItemsSource="{Binding}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Товар" 
                                            Binding="{Binding Product.Name}" 
                                            Width="*"/>
                        <DataGridTextColumn Header="Цена за ед." 
                                            Binding="{Binding UnitPrice, StringFormat={}{0} руб.}"
                                            Width="100"/>
                        <DataGridTextColumn Header="Количество" 
                                            Binding="{Binding Quantity}" 
                                            Width="80"/>
                        
                        <DataGridTemplateColumn Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Удалить" 
                                            Click="RemoveProductButton_Click"                                         
                                            FontSize="10"
                                            Height="20"
                                            Padding="5,0"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <Border Grid.Row="2" 
                        Background="LightYellow" 
                        Padding="10"
                        CornerRadius="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Общая сумма заказа:" 
                                   FontWeight="Bold"
                                   FontSize="14"
                                   VerticalAlignment="Center"/>

                        <TextBlock x:Name="TotalPriceText" 
                                   Grid.Column="1"
                                   Text="0.00 руб."
                                   FontWeight="Bold"
                                   FontSize="16"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </Grid>
        </GroupBox>

        <Border Grid.Row="2" 
                Padding="10">
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Content="Сохранить" 
                        Width="120" 
                        Height="35" 
                        Click="Save_Click" 
                        Margin="0,0,10,0"/>
                <Button Content="Отмена" 
                        Width="120" 
                        Height="35" 
                        Click="Cancel_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>